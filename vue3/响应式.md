vue3 Proxy 的 get 拦截是惰性的，只有在实际访问 o.obj2 时才会递归代理它。这是 Vue 3 响应式系统的设计（按需代理，避免不必要的性能开销）。
`var o = reactive({name: 1,obj2:{name: 110}})`
而 definePropty 是一次性全部设置为响应式
vue2 用闭包来保存每个属性的 dep 对象，而 vue3 使用一个全局的 weakMap 来保存

兼容 vue2 options 写法的代码在：核心实现在 runtime-core/src/componentOptions.ts 中的 applyOptions 函数：

```javascript
if (injectOptions) {
  resolveInjections(injectOptions, ctx, checkDuplicateProperties)
}

if (methods) {
  for (const key in methods) {
    const methodHandler = (methods as MethodOptions)[key]
    if (isFunction(methodHandler)) {
      ctx[key] = methodHandler.bind(publicThis)
    }
  }
}

if (dataOptions) {
  const data = dataOptions.call(publicThis, publicThis)
  instance.data = reactive(data)
}

// state initialization complete at this point - start caching access
shouldCacheAccess = true

if (computedOptions) {
  for (const key in computedOptions) {
    const opt = (computedOptions as ComputedOptions)[key]
    const get = isFunction(opt)
      ? opt.bind(publicThis, publicThis)
      : isFunction(opt.get)
        ? opt.get.bind(publicThis, publicThis)
        : NOOP
    const set =
      !isFunction(opt) && isFunction(opt.set)
        ? opt.set.bind(publicThis)
          : NOOP
    const c = computed({
      get,
      set,
    })
    Object.defineProperty(ctx, key, {
      enumerable: true,
      configurable: true,
      get: () => c.value,
      set: v => (c.value = v),
    })
  }
}

if (watchOptions) {
  for (const key in watchOptions) {
    createWatcher(watchOptions[key], ctx, publicThis, key)
  }
}
```

createApp()
app.mount()
patch
processComponent
if (setup) {
let result = setup()
if (result is function) instance.render = result
else instance.setupState = proxyRefs(setupResult);
}
finishComponentSetup()
if (!instance.render) instance.render = compile(template)

if (vue2) applyOptions(instance)
