vue3 中：

1.只有在 <script setup> 顶层直接定义的变量、函数会自动暴露，可以在模版中直接使用。嵌套在函数或块中的内容不会自动暴露。
无需手动导出：你不需要写 return { increment， count}，编译器会帮你完成。

与 Vue 2 的区别：Vue 2 需要显式定义 methods，而 <script setup> 通过自动暴露简化了这一步

2.watch 变得更加灵活

```javascript
const x = ref(0);
const y = ref(0);

// 单个 ref
watch(x, (newX) => {
  console.log(`x is ${newX}`);
});

// getter 函数
watch(
  () => x.value + y.value,
  (sum) => {
    console.log(`sum of x + y is: ${sum}`);
  }
);

// 多个来源组成的数组
watch([x, () => y.value], ([newX, newY]) => {
  console.log(`x is ${newX} and y is ${newY}`);
});

// 注意，你不能直接侦听响应式对象的属性值，例如:
const obj = reactive({ count: 0 });

// 错误，因为 watch() 得到的参数是一个 number
watch(obj.count, (count) => {
  console.log(`Count is: ${count}`);
});
// 这里需要用一个返回该属性的 getter 函数：
// 提供一个 getter 函数
watch(
  () => obj.count,
  (count) => {
    console.log(`Count is: ${count}`);
  }
);
```

```javascript
const todoId = ref(1);
const data = ref(null);

watch(
  todoId,
  async () => {
    const response = await fetch(
      `https://jsonplaceholder.typicode.com/todos/${todoId.value}`
    );
    data.value = await response.json();
  },
  { immediate: true }
);

// 这个例子中，回调会立即执行，不需要指定 immediate: true
watchEffect(async () => {
  const response = await fetch(
    `https://jsonplaceholder.typicode.com/todos/${todoId.value}`
  );
  data.value = await response.json();
});
```

watch vs. watchEffect​
watch 和 watchEffect 都能响应式地执行有副作用的回调。它们之间的主要区别是追踪响应式依赖的方式：

watch 只追踪明确侦听的数据源。它不会追踪任何在回调中访问到的东西。另外，仅在数据源确实改变时才会触发回调。watch 会避免在发生副作用时追踪依赖，因此，我们能更加精确地控制回调函数的触发时机。

watchEffect，则会在副作用发生期间追踪依赖。它会在同步执行过程中，自动追踪所有能访问到的响应式属性。这更方便，而且代码往往更简洁，但有时其响应性依赖关系会不那么明确。

3.vue3 中 transition 和 router-view

```javascript
<router-view class="view" v-slot="{ Component }">
    <transition name="fade" mode="out-in">
        <component :is="Component"/>
    </transition>
</router-view>
<style>
    .fade-enter-from,
    .fade-leave-to {
        opacity: 0;
    }
    .fade-enter-to,
    .fade-leave-from {
        opacity: 1;
    }
    .fade-enter-active,
    .fade-leave-active {
        transition: 0.5s ease;
    }
</style>
```

vue2

```javascript
<transition mode="out-in" name="slide-base">
  <router-view name="base" class="main-aside main-aside-base" />
</transition>
```

4.hooks 和 minix 的区别
Vue 2 的用户可能会对 mixins 选项比较熟悉。它也让我们能够把组件逻辑提取到可复用的单元里。然而 mixins 有三个主要的短板：

不清晰的数据来源：当使用了多个 mixin 时，实例上的数据属性来自哪个 mixin 变得不清晰，这使追溯实现和理解组件行为变得困难。这也是我们推荐在组合式函数中使用 ref + 解构模式的理由：让属性的来源在消费组件时一目了然。

命名空间冲突：多个来自不同作者的 mixin 可能会注册相同的属性名，造成命名冲突。若使用组合式函数，你可以通过在解构变量时对变量进行重命名来避免相同的键名。

隐式的跨 mixin 交流：多个 mixin 需要依赖共享的属性名来进行相互作用，这使得它们隐性地耦合在一起。而一个组合式函数的返回值可以作为另一个组合式函数的参数被传入，像普通函数那样。

slot 区别：

ref 使用区别

1. 性能优化
   优化点 Vue 2 Vue 3 提升效果
   响应式系统 基于 Object.defineProperty 基于 Proxy 支持动态属性增删、性能提升 40%+
   虚拟 DOM 全量 Diff 静态标记（PatchFlag） + 靶向更新 Diff 速度提升 200%+
   Tree-Shaking 整体打包，无法按需剔除无用代码 模块化设计，支持按需引入 打包体积减少 41%+
   SSR 优化 渲染速度较慢 静态节点直接输出字符串 SSR 速度提升 2-3x
2. 开发体验改进
   特性 Vue 2 Vue 3 优势
   Composition API Options API（代码分散） Composition API（逻辑聚合） 逻辑复用更灵活，代码更易维护
   TypeScript 支持 需额外适配 原生支持 TS 类型推断更完善，开发更安全
   Fragment 支持 单根节点限制 支持多根组件（Fragment） 减少无意义包裹层
   Teleport 组件 无 <Teleport to="body"> 灵活控制 DOM 挂载位置
   Suspense 组件 无 <Suspense> 异步组件加载状态管理 简化异步逻辑处理

3. 扩展性增强
   能力 Vue 2 Vue 3 意义
   自定义渲染器 修改源码侵入性强 通过 createRenderer 解耦 支持非 DOM 环境（如 Canvas）
   生命周期钩子 前缀 before/destroy 统一为 onBeforeMount 等 命名更一致，逻辑更清晰
   全局 API 改造 Vue.prototype 挂载 app.config.globalProperties 避免污染全局作用域
   指令生命周期 简化的钩子 完整的生命周期（类似组件） 指令开发更灵活

总结
Vue 3 的核心优势：
✅ 更快：响应式 + 虚拟 DOM 优化带来性能飞跃。
✅ 更小：Tree-Shaking 减少打包体积。
✅ 更强：Composition API + TS 支持提升代码组织能力。
✅ 更灵活：自定义渲染器、Teleport 等扩展能力。

vue3 优化点： 1.静态节点提升
2.shapeflag
